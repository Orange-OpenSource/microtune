# @package _global_
defaults:
  - base
  - _self_


info:  DF simulated with CVAE and ORIG Train, Eval, Test Pickles generation from original and simulated pickles

# Input pickle file to load simulated dataset
dataset_simu_import_file: workloads_CVAE_generated_full_dataset

# Output pickles files prefixes for original and dbsas datasets
simu:
  pickles_prefix: ${datasets_prefix}_cvae
