# Get conf by running --cfg job --resolve

# python run_dummy.py --cfg job 

defaults:
  - base  # ouput paths, verbosity, ...
  - workload/base
  - datasets: traintest-full
  - tuner: oracle
  - tuner@oracle: oracle
  - _self_

